# Next Steps (optional)
These are optional upgrades if you want to keep pushing reliability and polish:

- Add per-metric badges (LIVE/OFFICIAL/PROXY/FALLBACK) in the UI.
- Add TTL caching per endpoint (localStorage/IndexedDB) so refreshes on mobile are smoother.
- Add parser unit tests using saved sample payloads (small harness page).
- Add a strict Content Security Policy once inline CSS is minimized.
